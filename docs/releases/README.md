# Release Notes

This directory stores detailed release notes, one file per released version.

Use the following naming convention:

```txt
release-<version>.md
```

Example:

```txt
release-1.0.1.md
```

Keep `CHANGELOG.md`, if added later, as a short index or summary. Detailed
notes should remain here to avoid making the root changelog too large.
