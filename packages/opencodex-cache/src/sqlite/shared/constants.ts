/**
 * Shared constants used by the SQLite cache implementation.
 */
/** Identifier used by early cache versions for the implicit default source. */
export const LEGACY_DEFAULT_SOURCE_ID = "default";

/** Display name assigned to the first local Codex source. */
export const DEFAULT_SOURCE_NAME = "Default";
